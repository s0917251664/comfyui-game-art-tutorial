# ComfyUI 遊戲美術 AI 產圖工作流教學

寫給:習慣用聊天介面(ChatGPT / Midjourney / Gemini)產圖,現在想建立更精準、可控管的正式產線的遊戲美術。

目標:從「打字賭運氣」進化到「精準控制構圖、角色一致性、可重複修改」。

**現況/問題**:目前團隊設計師的產圖方式,是各自到 ChatGPT、Midjourney 這類平台聊天給提示、各自產圖。同一個角色、同一種美術風格,不同人、不同時間產出的結果完全不一致,也沒有可回溯的參數紀錄——這正是這條 ComfyUI 產線要解決的問題。

**定位說明**:ComfyUI 是這條產線裡的其中一個生成引擎/工具,**不是要每個人都得學會拉節點**。多數情境下,美術只需要打開別人已經存好的 workflow(見 `workflows/` 資料夾)、換掉文字或參考圖、按 Queue,結果就是一張圖——跟用其他軟體丟出一個檔案沒兩樣。真正需要碰節點編輯、理解底層原理的,是少數負責維護/調整這條產線的人(例如現在的你)。這份文件本身是寫給「維護產線的人」的,不代表整個團隊都要從第 0 章開始學。

---

## 第 0 章:名詞對照表(先收藏,看不懂就回來查)

| 專有名詞 | 用你熟悉的話講 |
|---|---|
| Checkpoint(底模) | 一個「畫風套件」,決定畫面整體風格與品質基礎 |
| Node(節點) | 一個個小方塊,每個方塊做一件事(讀模型、輸入文字、生成、存檔),接起來就是一條生產線 |
| Workflow(工作流) | 把節點連起來的整張圖,等於是「產圖的 SOP 流程圖」,可以存檔、分享、重複使用 |
| Prompt | 你打字描述想要什麼,跟聊天介面一樣 |
| Seed | 這次生成的「初稿版本編號」。同樣的 seed + 同樣的描述 = 構圖大致固定,不會每次整張重洗 |
| Steps / CFG | 生成的精細程度與「聽話程度」參數,先不用深究,用預設值即可 |
| Denoise(去噪強度) | 用在圖生圖時,決定「要保留原圖多少」。越低越像原圖,越高越像重畫 |
| Inpainting(局部重繪) | 像 Photoshop 套索選取一塊區域,只重畫這裡,其他地方不動 |
| ControlNet | 丟一張姿勢圖/線稿/深度圖進去,AI 照著這個構圖畫,不再是用嘴巴形容姿勢賭運氣 |
| LoRA | 訓練一個小外掛,讓 AI 記住「你的角色長相」或「你們的美術風格」,之後穩定套用 |
| IPAdapter | 不用訓練,直接丟一張參考圖說「照這張的風格/角色畫」,比 LoRA 快但沒那麼穩定 |
| VAE | 把 AI 內部的資料轉成看得懂的圖片(和把圖片轉成 AI 看得懂的資料),通常內建在底模裡不用管 |

---

## 第 0.5 章:先想清楚——你要哪種效果?(功能地圖 + 設備/預算選型)

寫給:不熟 AI 產圖原理、也還在建立美術判斷力的你。不用先搞懂每個模型的技術細節,**先問自己「我現在卡在哪一步」**,再對照下表找該用哪個技術、看哪一章。

### A. 功能地圖:情境 → 技術 → 章節

| 你現在的狀況 | 該用的技術 | 對應章節 |
|---|---|---|
| 還在發想,想憑空生出新圖、多看幾種可能性 | 文生圖(txt2img)+ 批次比較 | 第 3、4 章 |
| 手上已經有草稿/線稿,想要上色或精緻化,但不想丟掉自己畫的構圖 | 圖生圖(img2img),用 denoise 控制保留程度 | 第 5 章 |
| 圖生出來了,但某個地方崩壞(手、臉、道具穿模),想單獨修那一塊 | 局部重繪(Inpainting) | 第 6 章 |
| 姿勢、透視、構圖要精準,不想用文字形容賭運氣 | ControlNet(Canny 線稿 / Pose 骨架 / Depth 深度圖) | 第 7 章 |
| 同一個角色要出現在很多張圖裡,臉/服裝要「長得一樣」 | IPAdapter(快、不用訓練)或 LoRA(穩、需前置訓練) | 第 8 章 |
| 圖解析度不夠、細節不夠銳利,想放大加細節 | Upscale(放大模型 + 二次取樣補細節),透過 `generate.py upscale` 呼叫,細節見第 8.5 章 | 第 8.5 章 |
| 需要過場動畫、角色小動作素材 | 影片模型(Wan2.2)—Mac 上已有模型檔案,這台 Windows 機器目前還沒裝 | — |

判斷原則很簡單:**先確定「輸入」是什麼**。沒有輸入圖 → txt2img。有輸入圖但想整張重新詮釋 → img2img。有輸入圖但只想動一小塊 → Inpainting。有輸入圖是拿來當「構圖範本」而不是拿來重畫 → ControlNet。有輸入圖是拿來當「這是誰/這是什麼風格」的參考 → IPAdapter/LoRA。

### B. 現在:沒預算,本機免費跑(依顯卡等級選模型)

| 顯卡等級(VRAM) | 建議底模 | 說明 |
|---|---|---|
| ≤ 8GB(輕量) | SD1.5 系列微調(如 DreamShaper、RealisticVision) | 速度快、生態最成熟(LoRA/ControlNet最多),畫質天花板較低 |
| 12~16GB(**這台 RTX 4080 屬於這級**) | SDXL 系列(目前教學走的路線),或 SDXL 微調版(如 Juggernaut XL、RealVisXL)畫質再更好一點,workflow 完全不用改,單純換 checkpoint 檔名 | ControlNet / IPAdapter 生態最完整,目前性價比最高的選擇 |
| 12~16GB,想再往上衝畫質 | FLUX 蒸餾/輕量版(如 FLUX.1 schnell、Mac 上已有的 flux-2-klein-4b) | 畫質通常優於 SDXL,但 workflow 結構不同(要換 UNETLoader + T5 文字編碼器),ControlNet/LoRA 生態還在追上 |
| 24GB+(之後換卡或上雲端才會遇到) | FLUX.1 dev 完整版、SD3.5 Large | 更大量 LoRA 疊加、更大 batch,現階段用不到 |

你這台机器目前照教學文件裝的是 SDXL 底模,落在「性價比最高」那格,先把第 3~8 章的基本功練熟,之後真的覺得畫質是瓶頸,再考慮升級到 FLUX 系列,workflow 需要另外重建(不是換檔名就好)。

### C. 之後公司覺得這個工作模式有價值了:怎麼接外部大模型

好消息是 **ComfyUI 本身就內建了「API 節點」**,可以直接在同一張 workflow 裡呼叫外部商用模型的雲端 API,不用換工具、不用重建整條產線,只是把某個節點換成對應的 API 節點,再填入該服務的 API key。目前 ComfyUI 內建支援的相關服務包括:

| 類型 | 服務商 |
|---|---|
| 圖片生成/編輯 | BFL(FLUX Pro / Kontext 圖片編輯)、OpenAI(DALL-E / GPT-Image)、Google(Gemini / Imagen)、Ideogram、Recraft、Bria |
| 影片生成 | Kling、Luma、Runway、PixVerse、MiniMax、Vidu |
| 3D 資產 | Meshy、Rodin、Tripo、Hunyuan3D |

實務上常見的用法是**混合式產線**:平常用本機免費模型(SDXL)大量出草稿、篩方向;真正要交件的關鍵美術資產,才用一兩張額度呼叫 BFL/OpenAI 這類商用 API 精修到更高畫質——這樣預算花在刀口上,而不是整批圖都用昂貴 API 生成。等公司真的核准預算時,我可以再帶你申請對應的 API key、接上這些節點,workflow 架構不用重做。

---

## 第 1 章:本機環境快照

### 1a. Windows / RTX 4080(2026-08-14 建立,本機當前使用中)

你的機器:Windows 11,NVIDIA GeForce RTX 4080(16GB VRAM),driver 581.08。

已經幫你確認並建置好的環境:

| 項目 | 內容 |
|---|---|
| ComfyUI 位置 | `%USERPROFILE%\ComfyUI`(git 版本,追蹤官方最新版,方便之後裝 custom node) |
| Python 環境 | `%USERPROFILE%\ComfyUI\.venv`,Python 3.13.9,torch 2.13.0+cu130,**CUDA 加速已確認可用**(`torch.cuda.is_available()` → True,偵測到 RTX 4080)。特別注意:一開始裝的是 cu126 版,ComfyUI 啟動時會警告「20 系以上顯卡建議 cu130 以上才能用到最佳化 CUDA 運算」,已升級到 cu130 消除警告 |
| 啟動方式 | 終端機執行:`%USERPROFILE%\ComfyUI\.venv\Scripts\python.exe %USERPROFILE%\ComfyUI\main.py --listen 127.0.0.1 --port 8188`,或直接執行 `%USERPROFILE%\ComfyUI\start_comfyui.ps1` |
| 使用介面 | 啟動後瀏覽器打開 `http://127.0.0.1:8188` |
| 這台機器只有一套 ComfyUI | 沒有桌面版搶 port,所以直接用預設的 8188,不用像 Mac 那台改 8189 |
| 已裝 custom node | `ComfyUI-Manager`(套件商店介面)、`ComfyUI_IPAdapter_plus`(第 8 章用)、`comfyui_controlnet_aux`(第 7 章 Pose/Depth 前處理用,2026-08-17 補裝) |
| 已裝模型 | `models/checkpoints/sd_xl_base_1.0.safetensors`、`models/controlnet/controlnet-canny-sdxl-1.0.safetensors`、`models/controlnet/controlnet-depth-sdxl-1.0.safetensors`、`models/controlnet/controlnet-openpose-sdxl-1.0.safetensors`(後兩個 2026-08-17 補裝)、`models/ipadapter/ip-adapter-plus_sdxl_vit-h.safetensors`、`models/clip_vision/CLIP-ViT-H-14-laion2B-s32B-b79K.safetensors` —— 第 3~8 章需要的模型一次裝齊 |
| 已實測驗證 | 用 `workflows/test_workflow_api.json`(跟 Mac 那次一樣的 prompt)透過 API 送出生成,1024x1024 / 25 steps **只花約 9.6 秒**,明顯快於 Mac 的 MPS。產出圖存在 `output/教學測試_第一張圖_00001_.png` |
| 第 3~8 章 workflow 檔案 | 這台機器沒有桌面版,所以改成把 `Ch3_文生圖.json`、`Ch4_批次生成.json`、`Ch5_圖生圖.json`、`Ch6_局部重繪.json`、`Ch7_ControlNet.json`、`Ch8_IPAdapter.json` 直接放進 `%USERPROFILE%\ComfyUI\user\default\workflows\`,瀏覽器打開介面後點左上角選單 **Workflow → Open(或 Browse Workflows)** 就能看到這份章節目錄,選一個直接載入、按 Queue 就能跑。這幾個檔案在這個 repo 的 `workflows/` 資料夾裡也各存了一份,方便版本控管、之後換機器可以直接複製過去用 |

> 這台機器上模型是各自獨立下載的,**沒有**像 Mac 那台的共用模型庫(`~/ComfyUI-Shared/models`)概念,全部都在 `%USERPROFILE%\ComfyUI\models\` 底下對應子資料夾。之後新增模型也統一放在這裡。

### 1b. Mac / M3 Max(2026-07-29 建立,已實測跑通,舊機器記錄)

你的機器:MacBook Pro M3 Max,36GB 統一記憶體,macOS 26.5.2。

已經幫你確認並建置好的環境:

| 項目 | 內容 |
|---|---|
| ComfyUI 位置 | `~/ComfyUI`(git 版本,追蹤官方最新版,方便之後裝 custom node) |
| Python 環境 | `~/ComfyUI/.venv`,Python 3.13,torch 2.13.0,**MPS 加速已確認可用** |
| 啟動方式 | 終端機執行:`cd ~/ComfyUI && .venv/bin/python3 main.py --listen 127.0.0.1 --port 8189` |
| 使用介面 | 啟動後瀏覽器打開 `http://127.0.0.1:8189` |
| 另一套安裝 | `~/ComfyUI-Installs/ComfyUI` 是 ComfyUI **官方桌面版**,平常就一直背景常駐開著、佔用預設的 8188 port(這也是為什麼教學環境改用 **8189**,避免搶 port)。桌面版跟教學用的 git 版是**兩個獨立程式**,但共用同一份模型庫(見下方) |

### 重要發現:你機器上其實已經有一個「共享模型庫」

`~/ComfyUI-Shared/models` 是桌面版原本就設定好、用來集中存放模型的資料夾(桌面版啟動參數裡指定的 `--extra-model-paths-config`)。我已經幫教學用的 git 版也接上這個路徑(`~/ComfyUI/extra_model_paths.yaml`),**所以兩套 ComfyUI 現在共用同一批模型,不用重複下載**。

裡面盤點到你已經有的模型(非我今天新裝):

| 檔案 | 用途 |
|---|---|
| `flux-2-klein-4b.safetensors` | **FLUX.2 Klein 4B**,新一代文生圖模型,畫質通常比 SDXL 更好 |
| `wan2.2_fun_control_5B_bf16.safetensors` | **Wan2.2 5B** 影片生成模型,就是我們之前討論、你這台機器最適合跑的影片模型 |
| `wan2.2_vae.safetensors` / `flux2-vae.safetensors` / `ae.safetensors` | 上面兩個模型搭配用的 VAE |
| `umt5_xxl_fp8_e4m3fn_scaled.safetensors` | Wan2.2 用的文字編碼器 |

這代表你其實已經有能力玩文生圖(Flux)跟文生影片(Wan2.2)了,只是還沒有一套簡單好懂的 workflow 帶你操作 —— 這也是第 7~8 章之後可以直接銜接的方向,不用再額外下載大模型。

底模(本次新裝,教學用):

| 檔案 | 位置 |
|---|---|
| `sd_xl_base_1.0.safetensors`(SDXL,6.6GB) | `~/ComfyUI-Shared/models/checkpoints/` |

之所以第一課還是選 SDXL 而不是直接用已經有的 Flux,是因為 SDXL 的 workflow 只需要一個 Checkpoint 節點,結構最簡單,適合完全新手建立第一次的操作直覺;Flux/Wan 這類新模型節點較多、拆得比較細,放在後面章節熟悉基礎後再上手比較不會被嚇到。

> 之後你自己也可以裝「ComfyUI Manager」這個 custom node,它是一個介面化的套件商店,之後要裝 ControlNet、IPAdapter 相關節點時會方便很多(第 7、8 章會用到)。

> **已排除的重複檔案**:`qwen_3_4b.safetensors` 原本在 `~/ComfyUI/models/text_encoders/` 和 `~/ComfyUI-Shared/models/text_encoders/` 各有一份,檔案大小相同但內容不同。逐一比對 tensor 數值後發現 `~/ComfyUI` 那份裡有個權重(`model.layers.32.self_attn.v_proj.weight`)91.8% 是精確的 0,是下載中斷造成的損毀檔(旁邊還留著 `.aria2` 殘留控制檔可以佐證)。已刪除損毀的那份,只保留 `~/ComfyUI-Shared/models/` 裡健康的版本。
>
> **這件事也定調了以後模型該放哪**:`~/ComfyUI-Shared/models/` 是桌面版跟教學版共用的模型庫,**之後任何新模型都應該下載到這裡**(依類型分到對應子資料夾,如 `checkpoints/`、`loras/`、`text_encoders/`),不要分別裝在各自的 `models/` 資料夾裡,否則會再長出重複或版本不一致的檔案。

---

## 第 2 章:第一次啟動與介面導覽

1. 打開終端機,執行第 1 章的啟動指令
2. 瀏覽器打開 `http://127.0.0.1:8189`
3. 畫面上會看到一堆用線連起來的方塊,這就是「工作流」,預設會有一組現成的文生圖流程

**先不要慌**,你只需要認得這幾個方塊:

- **Load Checkpoint**:選你要用哪個「畫風套件」(選我們剛裝的 sd_xl_base_1.0)
- **CLIP Text Encode(正向)**:你想要的東西,打字描述
- **CLIP Text Encode(負向)**:你不想要的東西(例如「模糊、多手指」)
- **KSampler**:實際生成的引擎,裡面有 seed、steps 等參數
- **VAE Decode**:把結果轉成看得懂的圖片
- **Save Image**:存檔

右下角有個 **Queue(佇列)** 按鈕,按下去就是「送出生成」,等同聊天介面按送出。

---

## 第 3 章:第一組工作流 —— 文生圖(txt2img)

> **對應 workflow 檔案:`Ch3_文生圖.json`**,已經放進桌面版,打開桌面版 → Workflow → Open → 選 `Ch3_文生圖`,直接按 Queue 就能跑,不用自己拉節點。

**練習**:
1. 在正向 Text Encode 打入你想畫的東西(例如:一個穿盔甲的女戰士,站姿,遊戲角色設計,乾淨背景)
2. 負向留預設或打 `blurry, low quality, extra fingers`
3. 按 Queue,等它跑完

**重點體驗**:跑完後看 KSampler 裡的 seed 數字。把這個數字**鎖住**(取消隨機),只改文字裡的一個形容詞重跑一次 —— 你會發現構圖骨架大致不變,只有細節在變。這是聊天介面做不到的控制力。

> **已實測驗證**:這條流程已經幫你實際跑過一次,結果存在 `~/ComfyUI-遊戲美術教學/第一張測試圖.png`,用的 prompt 是:
> `a female game character concept art, wearing fantasy armor, standing pose, clean studio background, digital painting, highly detailed`
> 負向:`blurry, low quality, extra fingers, deformed, watermark`
> 你可以在瀏覽器介面打一樣的文字、選同一個 checkpoint(sd_xl_base_1.0),對照看看自己跑出來的跟這張像不像,確認環境完全沒問題。

---

## 第 4 章:Seed 與批次的概念

> **對應 workflow 檔案:`Ch4_批次生成.json`**,batch_size 已設成 4、seed 設成 randomize,開起來按一次 Queue 就能一次看 4 張不同版本比較。

- Seed 隨機 = 每次都是全新的抽卡,適合「還在發想階段,想要多樣性」
- Seed 固定 = 微調同一張圖(換顏色、換光線、換小細節),適合「已經選定方向,要微調」
- ComfyUI 可以設定「batch」一次跑多張不同 seed 的圖,一次看多個版本,不用一張一張來回跟 AI 對話

---

## 第 5 章:圖生圖(img2img)—— 拿自己的草稿當底圖

> **對應 workflow 檔案:`Ch5_圖生圖.json`**,打開後點 LoadImage 節點換成你自己的草稿,denoise 預設 0.6,可自行調整。

流程:把 Load Checkpoint 之後的 Empty Latent Image 換成 **Load Image**(讀你的草稿)+ **VAE Encode**,再接到 KSampler。

**Denoise 參數是關鍵**:
- 0.3~0.4:大致保留你的構圖,AI 幫你上色/加細節
- 0.6~0.7:保留大致構圖,細節大幅改變
- 0.9 以上:幾乎等於重畫,只留一點點原圖影子

這一步可以把你手繪的草稿或角色設計圖,直接轉成上色完稿,而不是用文字重新形容一次。

---

## 第 6 章:局部重繪(Inpainting)—— 解決「來回改圖」的痛點

> **對應 workflow 檔案:`Ch6_局部重繪.json`**,載入圖片後在 LoadImage 節點上右鍵選「Open in MaskEditor」塗遮罩,denoise 預設 1.0(只有塗黑處會被重畫)。

這是你原本聊天介面工作流程裡**最痛的環節**的解方。

流程:載入圖片 → 用內建的遮罩工具(滑鼠塗抹)圈出要修改的區域 → 送到 KSampler(搭配 inpaint 相關節點)→ 只有你塗的區域會被重新生成,其他地方原封不動。

**練習**:拿一張你之前用聊天介面產出、但某個地方畫壞的圖(例如手部崩壞),丟進來,只塗手的部分,重新生成,體會「其他地方完全不受影響」的差異。

---

## 第 7 章:精準構圖控制 —— ControlNet

> **對應 workflow 檔案:`Ch7_ControlNet.json`**,已實測跑成功。用的是 Canny(邊緣偵測),載入你的線稿/照片後會自動抓邊緣當構圖依據,`ControlNetApplyAdvanced` 節點上的 `strength` 越高,構圖跟參考圖越像。

**這是「精準度」問題的核心解方。**

流程概念:
1. 準備一張姿勢參考(可以是照片、簡筆骨架圖,甚至你自己的草稿線稿)
2. 加入 ControlNet 相關節點(需要額外下載 ControlNet 模型,+ Load ControlNet Model 節點),指定用 Pose(姿勢)、Depth(深度)或 Lineart(線稿)哪種控制方式
3. 生成結果會照著參考圖的構圖/姿勢走,文字只負責描述風格和細節

**這樣用**:你可以自己擺拍或簡單畫一個火柴人骨架,精準指定角色姿勢,而不是打字形容「舉起右手往前跨步」賭 AI 理解對不對。

> 這步需要額外下載 ControlNet 模型(依 SDXL 版本,一個約 2~5GB),建議先裝 ComfyUI Manager 再透過介面搜尋安裝,會比手動下載省事很多。
>
> **2026-08-17 更新**:Pose(骨架姿勢)、Depth(深度圖)這兩種控制方式已經裝好,`tools_src/generate.py` 的 `pose_only`/`character_action` task 現在支援 `--control-type canny|pose|depth` 參數選擇——`pose` 適合拿人物照片鎖動作(用 `comfyui_controlnet_aux` 的 `OpenposePreprocessor` 抽骨架,忽略衣服/背景細節),`depth` 適合鎖空間前後景關係。對應的 `Ch7b_ControlNet_Pose.json`、`Ch7c_ControlNet_Depth.json` workflow 檔案也補好了,結構跟 `Ch7_ControlNet.json` 一樣、只是把 Canny 節點換成對應的前處理節點,給喜歡在 ComfyUI 網頁介面手動開的人用。

---

## 第 8 章:角色與風格一致性 —— IPAdapter / LoRA

> **對應 workflow 檔案:`Ch8_IPAdapter.json`**,已實測跑成功。載入一張角色/風格參考圖,`IPAdapterAdvanced` 節點的 `weight` 控制貼合強度(建議先從 0.8 試),數值越高越像參考圖。LoRA 訓練屬於進階項目,還沒有現成 workflow(見下方附錄)。

**問題**:聊天介面每次產圖角色臉都不一樣,沒辦法用在同一個遊戲角色的不同姿勢/場景上。

**兩種解法,由輕到重:**

- **IPAdapter(快,不用訓練)**:丟一張角色參考圖進工作流,AI 會盡量貼合這張圖的臉/風格特徵,適合快速測試或還沒定案時用
- **LoRA(穩,需要前置訓練)**:把角色設定圖(建議 15~30 張不同角度)或你們團隊的美術風格圖集,用另一個訓練工具(常見的是 kohya_ss)訓練成一個小模型檔案,之後在 ComfyUI 裡直接套用,穩定度遠高於 IPAdapter,適合已經定案、要大量產出的正式階段

**建議順序**:定案前用 IPAdapter 快速測試方向,定案後投資時間訓練 LoRA,穩定產出整套遊戲素材。

---

## 第 8.5 章:放大精修 —— Upscale

> **對應指令:`generate.py upscale`**,2026-08-17 補上,已實測跑成功(1024×1024 → 2048×2048)。目前沒有對應的 workflow JSON 給人在 ComfyUI 介面手動開,只能透過 `generate.py` 使用。

**這是最後一道工序,不是產圖的一部分。** 前面幾章生出來的圖(`concept`/`character_action`/`refine` 這些)解析度都固定在 `device_config.json` 裡設定的預設值(這台機器是 1024×1024),平常拿來篩方向、比對構圖夠用,但真的要交件、要印出來、要當主視覺放大用時,解析度不夠、細節也會模糊。Upscale 做兩件事:

1. **放大模型**(這裡用的是 `4x-UltraSharp.pth`,適合插畫/概念圖風格):把圖片像素數變多,邊緣盡量保持銳利,比單純縮放好
2. **二次取樣補細節**(`--denoise` 控制強度,預設 0.4):放大完之後用同一個底模、同樣的 prompt 再跑一次擴散,補回原本解析度不夠時畫不出來的細節(材質、毛髮、盔甲紋路),單純放大模型本身不會生出新細節,這一步才會

**這樣用**:挑一張已經確定要用的圖,原封不動把當初生成用的 prompt 帶進來(這樣二次取樣時風格才會一致),指定要放大到幾倍(預設 2 倍),`denoise` 太低細節補不夠、太高會偏離原圖構圖,抓 0.3~0.5 之間微調。

---

## 第 9 章:建立你的生產線 SOP

1. 把調好的工作流用右上角 **Save** 存成 `.json`,放進 `~/ComfyUI/user/default/workflows/`,下次打開 ComfyUI 直接從選單載入
2. 把這個 json 分享給組員,大家用同一套流程,確保產出風格一致、可回溯——**組員不需要學會拉節點**,拿到 json 打開後只要換文字/換參考圖、按 Queue 就好,跟操作一個現成工具沒兩樣。需要新增/調整節點結構(例如換模型、加新的控制方式)才是本文件負責的範圍
3. 需要一次產一批素材(例如 10 種武器材質變體)時,用 batch 功能一次排隊生成,不用一張一張手動跑

---

## 第 10 章:Mac 使用注意事項

- Mac 用 MPS(蘋果的 GPU 加速)跑,沒有 N 卡的 xformers / flash-attention 加速,同樣工作流速度會比同級距 N 卡慢,這是先天限制
- 36GB 統一記憶體對 SDXL、ControlNet、輕量影片模型(如 Wan2.2 5B)都夠用,但同時開很多大型模型或跑 14B 級影片模型會吃緊,建議一次專心跑一個大任務
- 每次重開機後,啟動 ComfyUI 記得用第 1 章的指令重新啟動伺服器

---

## 附錄:環境安裝紀錄(2026-07-29)

已經裝好、可以直接用的東西:

| 章節 | 項目 | 存放位置 |
|---|---|---|
| 第 3~6 章 | SDXL 底模 | `~/ComfyUI-Shared/models/checkpoints/sd_xl_base_1.0.safetensors` |
| 第 7 章 | ControlNet(Canny, SDXL 版) | `~/ComfyUI-Shared/models/controlnet/controlnet-canny-sdxl-1.0.safetensors` |
| 第 8 章 | ComfyUI_IPAdapter_plus(custom node) | `~/ComfyUI-Installs/ComfyUI/ComfyUI/custom_nodes/ComfyUI_IPAdapter_plus` |
| 第 8 章 | CLIP Vision 編碼器 | `~/ComfyUI-Shared/models/clip_vision/CLIP-ViT-H-14-laion2B-s32B-b79K.safetensors` |
| 第 8 章 | IPAdapter 模型(Plus, SDXL 版) | `~/ComfyUI-Shared/models/ipadapter/ip-adapter-plus_sdxl_vit-h.safetensors` |

還沒裝、屬於進階項目:

| 項目 | 說明 |
|---|---|
| LoRA 訓練工具(如 kohya_ss) | 需要另外安裝設定,適合角色/風格定案後,想要最高穩定度時再做 |
| 其他 ControlNet 類型(Pose、Depth) | 目前只裝了 Canny,之後有需要可以比照同樣方式再加裝 |

**下一步建議**:先照第 2、3 章實際跑出第一張圖,有問題隨時可以再回來問。

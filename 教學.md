# ComfyUI 遊戲美術 AI 產圖工作流教學

寫給:習慣用聊天介面(ChatGPT / Midjourney / Gemini)產圖,現在想建立更精準、可控管的正式產線的遊戲美術。

目標:從「打字賭運氣」進化到「精準控制構圖、角色一致性、可重複修改」。

**現況/問題**:目前團隊設計師的產圖方式,是各自到 ChatGPT、Midjourney 這類平台聊天給提示、各自產圖。同一個角色、同一種美術風格,不同人、不同時間產出的結果完全不一致,也沒有可回溯的參數紀錄——這正是這條 ComfyUI 產線要解決的問題。

**定位說明**:ComfyUI 是這條產線裡的其中一個生成引擎/工具,**不是要每個人都得學會拉節點**。多數情境下,美術只需要打開已安裝機器的 ComfyUI `user/default/workflows/`，或另行取得的 workflow JSON，換掉文字或參考圖、按 Queue,結果就是一張圖——跟用其他軟體丟出一個檔案沒兩樣。repository 的 `workflows/` 只保留本機維護者的參考檔，已被 `.gitignore` 排除，不會跟著乾淨的 `git clone` 提供；真正需要碰節點編輯、理解底層原理的,是少數負責維護/調整這條產線的人(例如現在的你)。這份文件本身是寫給「維護產線的人」的,不代表整個團隊都要從第 0 章開始學。

---

## 第 0 章:名詞對照表(先收藏,看不懂就回來查)

| 專有名詞 | 用你熟悉的話講 |
|---|---|
| Checkpoint(底模) | 一個「畫風套件」,決定畫面整體風格與品質基礎 |
| Node(節點) | 一個個小方塊,每個方塊做一件事(讀模型、輸入文字、生成、存檔),接起來就是一條生產線 |
| Workflow(工作流) | 把節點連起來的整張圖,等於是「產圖的 SOP 流程圖」,可以存檔、分享、重複使用 |
| Prompt | 你打字描述想要什麼,跟聊天介面一樣 |
| Seed | 這次生成的「初稿版本編號」。同樣的 seed + 同樣的描述 = 構圖大致固定,不會每次整張重洗 |
| Steps / CFG | 生成的精細程度與「聽話程度」參數,先不用深究,用預設值即可 |
| Denoise(去噪強度) | 用在圖生圖時,決定「要保留原圖多少」。越低越像原圖,越高越像重畫 |
| Inpainting(局部重繪) | 像 Photoshop 套索選取一塊區域,只重畫這裡,其他地方不動 |
| ControlNet | 丟一張姿勢圖/線稿/深度圖進去,AI 照著這個構圖畫,不再是用嘴巴形容姿勢賭運氣 |
| LoRA | 訓練一個小外掛,讓 AI 記住「你的角色長相」或「你們的美術風格」,之後穩定套用 |
| IPAdapter | 不用訓練,直接丟一張參考圖說「照這張的風格/角色畫」,比 LoRA 快但沒那麼穩定 |
| VAE | 把 AI 內部的資料轉成看得懂的圖片(和把圖片轉成 AI 看得懂的資料),通常內建在底模裡不用管 |

---

## 第 0.5 章:先想清楚——你要哪種效果?(功能地圖 + 設備/預算選型)

寫給:不熟 AI 產圖原理、也還在建立美術判斷力的你。不用先搞懂每個模型的技術細節,**先問自己「我現在卡在哪一步」**,再對照下表找該用哪個技術、看哪一章。

### A. 功能地圖:情境 → 技術 → 章節

| 你現在的狀況 | 該用的技術 | 對應章節 |
|---|---|---|
| 還在發想,想憑空生出新圖、多看幾種可能性 | 文生圖(txt2img)+ 批次比較 | 第 3、4 章 |
| 手上已經有草稿/線稿,想要上色或精緻化,但不想丟掉自己畫的構圖 | 圖生圖(img2img),用 denoise 控制保留程度 | 第 5 章 |
| 圖生出來了,但某個地方崩壞(手、臉、道具穿模),想單獨修那一塊 | 局部重繪(Inpainting) | 第 6 章 |
| 姿勢、透視、構圖要精準,不想用文字形容賭運氣 | ControlNet(Canny 線稿 / Pose 骨架 / Depth 深度圖) | 第 7 章 |
| 要局部重繪,但那個區域「結構(關節/輪廓)不能變、外觀要換」(換武器要保持握姿、換材質紋路要保持造型)| 局部重繪 + ControlNet 結構鎖定(`guided_inpaint`) | 第 7.5 章 |
| 同一個角色要出現在很多張圖裡,臉/服裝要「長得一樣」 | IPAdapter(快、不用訓練)或 LoRA(穩、需前置訓練) | 第 8 章 |
| 圖解析度不夠、細節不夠銳利,想放大加細節 | Upscale(放大模型 + 二次取樣補細節),透過 `generate.py upscale` 呼叫,細節見第 8.5 章 | 第 8.5 章 |
| 要做單一小型 UI 圖示/物件素材(按鈕圖示、道具圖示、複合元件裡的單一構件),不是整個 UI 畫面 | `icon_asset`,永遠去背輸出透明背景 | 第 8.6 章 |
| 已經有一張定稿合成圖,想事後拆出幾個大塊區域各自疊放/調色 | `layer_split`,依遮罩裁切透明度,不重新生成內容 | 第 8.6 章 |
| 需要過場動畫、角色小動作、讓已過關的靜幀動起來 | `img2video` / `fx_loop` / `transition` / `clip_extend` / `camera_move`,見第 8.8 章 | 第 8.8 章 |
| 要這個角色去做別的動作/換場景,第一幀不必是那張定稿圖 | `character_video`(對應靜態 `style_lock`),見第 8.8 章 | 第 8.8 章 |
| 用一段動作影片驅動已經 lock 的角色 | `pose_drive`(對應靜態 `character_action`),見第 8.8 章 | 第 8.8 章 |

判斷原則很簡單:**先確定「輸入」是什麼**。沒有輸入圖 → txt2img。有輸入圖但想整張重新詮釋 → img2img。有輸入圖但只想動一小塊 → Inpainting。有輸入圖是拿來當「構圖範本」而不是拿來重畫 → ControlNet。有輸入圖是拿來當「這是誰/這是什麼風格」的參考 → IPAdapter/LoRA。

### B. 現在:沒預算,本機免費跑(依顯卡等級選模型)

| 顯卡等級(VRAM) | 建議底模 | 說明 |
|---|---|---|
| < 8GB(輕量) | SD1.5 系列微調(如 DreamShaper、RealisticVision) | 速度快、生態最成熟(LoRA/ControlNet最多),畫質天花板較低；本 repo 目前只承諾 concept/refine/一般 inpaint/upscale 與不帶參考圖的 icon_asset 基礎路徑，SDXL 版 ControlNet/IPAdapter/風格底模不可直接沿用 |
| 8GB～<12GB | SDXL light（預設 768×768） | `detect_device.py` 以 8000MB 為門檻；能走 SDXL add-on，但需保守控制解析度與 batch，避免 OOM |
| 12GB～<24GB（**這台 RTX 4080 屬於這級**） | `sdxl`，SDXL 1024×1024；也可選 SDXL 微調版(如 Juggernaut XL、RealVisXL) | `detect_device.py` 以 12000MB 為門檻，16～<24GB 也仍選這個 tier；ControlNet / IPAdapter 生態最完整,目前性價比最高 |
| 24GB+（本腳本預設） | `sdxl_high`，SDXL 1024×1024 | `detect_device.py` 會選這個 tier；沿用已驗證 SDXL graph，並提供較多 batch／高解析餘裕 |
| 12GB+（另行評估的新路線，不是自動 tier） | FLUX 蒸餾/輕量版、FLUX.1 dev、SD3.5 Large | workflow 與模型家族不同；不論 VRAM 落在哪個 SDXL tier，都不能把它視為自動替代品 |

你這台机器目前照教學文件裝的是 SDXL 底模,落在「性價比最高」那格,先把第 3~8 章的基本功練熟,之後真的覺得畫質是瓶頸,再考慮升級到 FLUX 系列,workflow 需要另外重建(不是換檔名就好)。

**2026-08-19 更新:`generate.py` 加了 `--style` 選填參數,不用重裝機器就能直接切換 SDXL 微調版底模**(選配,不給就維持這台機器裝機時鎖定的預設 checkpoint):`--style realistic` 對應 Juggernaut XL(寫實)、`--style illustration` 對應 Illustrious XL(插畫/概念藝術)、`--style anime` 對應 Pony Diffusion V6 XL(二次元/動漫)。這是因應遊戲美術不一定要寫實風格而補上的——之前這張表只提到寫實向的微調版,忽略了插畫/二次元也是常見需求。三個 checkpoint 都要另外下載才能用(概估大小/授權注意事項見 `skills/comfyui-install/reference/models.md`「選用風格底模」),裝機時預設不裝。另外多了一個 `--rating safe|questionable|explicit` 選項,只在 `--style anime`/`illustration` 時有效(這兩顆底模訓練資料本身就用分級標籤控制內容尺度),不給就完全不影響 prompt,細節見 `skills/comfyui-art-gen/reference/full-params.md`。**已實測驗證(2026-08-19~21)**:三顆風格底模都已下載並實測過。`--style illustration --rating safe` 正常出圖。`--style realistic` 不需要特殊 prompt 慣例,預設參數直接出圖正常,官方建議解析度 832x1216 直式(跟這台機器 `sdxl` tier 預設的 1024x1024 不同)。`--style anime`(Pony)**prompt 必須帶 `score_9, score_8_up, score_7_up`(至少 3 個 score 標籤)這組官方建議的品質標籤,不然輸出會不穩定(實測過灰階、跟描述無關的構圖)**——這不是程式碼的限制,是 Pony 這顆底模的固定用法,`skills/comfyui-art-gen/SKILL.md` 用到 `--style anime` 時要記得在 prompt 加這組標籤。細節見 `skills/comfyui-install/reference/models.md`「使用眉角」小節。

上面是特定機器的歷史實測紀錄，不是每次 clone 後都已具備的版本保證。可重現性以 [`docs/tested-versions.md`](docs/tested-versions.md) 的 manifest 為準；XU-Nano-PC 的實際 capture 已完成並標記 `verified`，其他機器若尚未擷取則維持 `pending_on_installed_machine`，未查證的 commit、套件版本與模型 hash 不填猜測值。

### C. 之後公司覺得這個工作模式有價值了:怎麼接外部大模型

好消息是 **ComfyUI 本身就內建了「API 節點」**,可以直接在同一張 workflow 裡呼叫外部商用模型的雲端 API,不用換工具、不用重建整條產線,只是把某個節點換成對應的 API 節點,再填入該服務的 API key。目前 ComfyUI 內建支援的相關服務包括:

| 類型 | 服務商 |
|---|---|
| 圖片生成/編輯 | BFL(FLUX Pro / Kontext 圖片編輯)、OpenAI(DALL-E / GPT-Image)、Google(Gemini / Imagen)、Ideogram、Recraft、Bria |
| 影片生成 | Kling、Luma、Runway、PixVerse、MiniMax、Vidu |
| 3D 資產 | Meshy、Rodin、Tripo、Hunyuan3D |

實務上常見的用法是**混合式產線**:平常用本機免費模型(SDXL)大量出草稿、篩方向;真正要交件的關鍵美術資產,才用一兩張額度呼叫 BFL/OpenAI 這類商用 API 精修到更高畫質——這樣預算花在刀口上,而不是整批圖都用昂貴 API 生成。等公司真的核准預算時,我可以再帶你申請對應的 API key、接上這些節點,workflow 架構不用重做。

---

## 第 1 章:本機環境快照

### 1a. Windows / RTX 4080(2026-08-14 建立,本機當前使用中)

你的機器:Windows 11,NVIDIA GeForce RTX 4080(16GB VRAM),driver 581.08。

已經幫你確認並建置好的環境:

| 項目 | 內容 |
|---|---|
| ComfyUI 位置 | `%USERPROFILE%\ComfyUI`(git 版本；這是當時追蹤最新版的歷史快照，現在重建必須依版本 manifest 鎖定已驗證 commit) |
| Python 環境 | `%USERPROFILE%\ComfyUI\.venv`,Python 3.13.9,torch 2.13.0+cu130,**CUDA 加速已確認可用**(`torch.cuda.is_available()` → True,偵測到 RTX 4080)。特別注意:一開始裝的是 cu126 版,ComfyUI 啟動時會警告「20 系以上顯卡建議 cu130 以上才能用到最佳化 CUDA 運算」,已升級到 cu130 消除警告 |
| 啟動方式 | 終端機執行:`%USERPROFILE%\ComfyUI\.venv\Scripts\python.exe %USERPROFILE%\ComfyUI\main.py --listen 127.0.0.1 --port 8188`,或直接執行 `%USERPROFILE%\ComfyUI\start_comfyui.ps1` |
| 使用介面 | 啟動後瀏覽器打開 `http://127.0.0.1:8188` |
| 這台機器只有一套 ComfyUI | 沒有桌面版搶 port,所以直接用預設的 8188,不用像 Mac 那台改 8189 |
| 已裝 custom node | `ComfyUI-Manager`(套件商店介面)、`ComfyUI_IPAdapter_plus`(第 8 章用)、`comfyui_controlnet_aux`(第 7 章 Pose/Depth 前處理用,2026-08-17 補裝) |
| LoRA 訓練工具 | `kohya_ss`(裝在 `~/kohya_ss`,獨立於 ComfyUI 之外,`uv sync` 建環境),2026-08-17 補裝,已實測跑過一次完整訓練驗證流程通,細節見第 8 章 |
| 已裝模型 | `models/checkpoints/sd_xl_base_1.0.safetensors`、`models/controlnet/controlnet-canny-sdxl-1.0.safetensors`、`models/controlnet/controlnet-depth-sdxl-1.0.safetensors`、`models/controlnet/controlnet-openpose-sdxl-1.0.safetensors`(後兩個 2026-08-17 補裝)、`models/ipadapter/ip-adapter-plus_sdxl_vit-h.safetensors`、`models/clip_vision/CLIP-ViT-H-14-laion2B-s32B-b79K.safetensors` —— 第 3~8 章需要的模型一次裝齊 |
| 已實測驗證 | 用已安裝機器上的本機 workflow（歷史紀錄檔名為 `workflows/test_workflow_api.json`）透過 API 送出生成,1024x1024 / 25 steps **只花約 9.6 秒**,明顯快於 Mac 的 MPS。產出圖存在 `output/教學測試_第一張圖_00001_.png`；這筆紀錄不能當成乾淨 clone 已完成實機驗證。 |
| 第 3~8 章 workflow 檔案 | 這台機器沒有桌面版,所以改成把各章節對應的 workflow JSON 直接放進 `%USERPROFILE%\ComfyUI\user\default\workflows\`,瀏覽器打開介面後點左上角選單 **Workflow → Open(或 Browse Workflows)** 就能看到這份章節目錄,選一個直接載入、按 Queue 就能跑。這些檔案是本機產物，repository 的 `workflows/` 僅供維護者參考且被 `.gitignore` 排除；換機器要用的話,自己手動複製或重新匯出,不會跟著 `git clone` 一起來 |

> 這台機器上模型是各自獨立下載的,**沒有**像 Mac 那台的共用模型庫(`~/ComfyUI-Shared/models`)概念,全部都在 `%USERPROFILE%\ComfyUI\models\` 底下對應子資料夾。之後新增模型也統一放在這裡。

### 1b. Mac / M3 Max(2026-07-29 建立,已實測跑通,舊機器記錄)

你的機器:MacBook Pro M3 Max,36GB 統一記憶體,macOS 26.5.2。

已經幫你確認並建置好的環境:

| 項目 | 內容 |
|---|---|
| ComfyUI 位置 | `~/ComfyUI`(git 版本；這是當時追蹤最新版的歷史快照，現在重建必須依版本 manifest 鎖定已驗證 commit) |
| Python 環境 | `~/ComfyUI/.venv`,Python 3.13,torch 2.13.0,**MPS 加速已確認可用** |
| 啟動方式 | 終端機執行:`cd ~/ComfyUI && .venv/bin/python3 main.py --listen 127.0.0.1 --port 8189` |
| 使用介面 | 啟動後瀏覽器打開 `http://127.0.0.1:8189` |
| 另一套安裝 | `~/ComfyUI-Installs/ComfyUI` 是 ComfyUI **官方桌面版**,平常就一直背景常駐開著、佔用預設的 8188 port(這也是為什麼教學環境改用 **8189**,避免搶 port)。桌面版跟教學用的 git 版是**兩個獨立程式**,但共用同一份模型庫(見下方) |

### 重要發現:你機器上其實已經有一個「共享模型庫」

`~/ComfyUI-Shared/models` 是桌面版原本就設定好、用來集中存放模型的資料夾(桌面版啟動參數裡指定的 `--extra-model-paths-config`)。我已經幫教學用的 git 版也接上這個路徑(`~/ComfyUI/extra_model_paths.yaml`),**所以兩套 ComfyUI 現在共用同一批模型,不用重複下載**。

裡面盤點到你已經有的模型(非我今天新裝):

| 檔案 | 用途 |
|---|---|
| `flux-2-klein-4b.safetensors` | **FLUX.2 Klein 4B**,新一代文生圖模型,畫質通常比 SDXL 更好 |
| `wan2.2_fun_control_5B_bf16.safetensors` | **Wan2.2 5B** 影片生成模型,就是我們之前討論、你這台機器最適合跑的影片模型 |
| `wan2.2_vae.safetensors` / `flux2-vae.safetensors` / `ae.safetensors` | 上面兩個模型搭配用的 VAE |
| `umt5_xxl_fp8_e4m3fn_scaled.safetensors` | Wan2.2 用的文字編碼器 |

這代表你其實已經有能力玩文生圖(Flux)跟文生影片(Wan2.2)了,只是還沒有一套簡單好懂的 workflow 帶你操作 —— 這也是第 7~8 章之後可以直接銜接的方向,不用再額外下載大模型。

底模(本次新裝,教學用):

| 檔案 | 位置 |
|---|---|
| `sd_xl_base_1.0.safetensors`(SDXL,6.6GB) | `~/ComfyUI-Shared/models/checkpoints/` |

之所以第一課還是選 SDXL 而不是直接用已經有的 Flux,是因為 SDXL 的 workflow 只需要一個 Checkpoint 節點,結構最簡單,適合完全新手建立第一次的操作直覺;Flux/Wan 這類新模型節點較多、拆得比較細,放在後面章節熟悉基礎後再上手比較不會被嚇到。

> 之後你自己也可以裝「ComfyUI Manager」這個 custom node,它是一個介面化的套件商店,之後要裝 ControlNet、IPAdapter 相關節點時會方便很多(第 7、8 章會用到)。

> **已排除的重複檔案**:`qwen_3_4b.safetensors` 原本在 `~/ComfyUI/models/text_encoders/` 和 `~/ComfyUI-Shared/models/text_encoders/` 各有一份,檔案大小相同但內容不同。逐一比對 tensor 數值後發現 `~/ComfyUI` 那份裡有個權重(`model.layers.32.self_attn.v_proj.weight`)91.8% 是精確的 0,是下載中斷造成的損毀檔(旁邊還留著 `.aria2` 殘留控制檔可以佐證)。已刪除損毀的那份,只保留 `~/ComfyUI-Shared/models/` 裡健康的版本。
>
> **這件事也定調了以後模型該放哪**:`~/ComfyUI-Shared/models/` 是桌面版跟教學版共用的模型庫,**之後任何新模型都應該下載到這裡**(依類型分到對應子資料夾,如 `checkpoints/`、`loras/`、`text_encoders/`),不要分別裝在各自的 `models/` 資料夾裡,否則會再長出重複或版本不一致的檔案。

---

## 第 2 章:第一次啟動與介面導覽

1. 打開終端機,執行第 1 章的啟動指令
2. 瀏覽器打開 `local_config.json` 的 `comfyui_url`（這台 Mac 歷史紀錄是 `http://127.0.0.1:8189`，其他機器不要直接照抄 port）
3. 畫面上會看到一堆用線連起來的方塊,這就是「工作流」,預設會有一組現成的文生圖流程

**先不要慌**,你只需要認得這幾個方塊:

- **Load Checkpoint**:選你要用哪個「畫風套件」(選我們剛裝的 sd_xl_base_1.0)
- **CLIP Text Encode(正向)**:你想要的東西,打字描述
- **CLIP Text Encode(負向)**:你不想要的東西(例如「模糊、多手指」)
- **KSampler**:實際生成的引擎,裡面有 seed、steps 等參數
- **VAE Decode**:把結果轉成看得懂的圖片
- **Save Image**:存檔

右下角有個 **Queue(佇列)** 按鈕,按下去就是「送出生成」,等同聊天介面按送出。

---

## 第 3 章:第一組工作流 —— 文生圖(txt2img)

**練習**:
1. 在正向 Text Encode 打入你想畫的東西(例如:一個穿盔甲的女戰士,站姿,遊戲角色設計,乾淨背景)
2. 負向留預設或打 `blurry, low quality, extra fingers`
3. 按 Queue,等它跑完

**重點體驗**:跑完後看 KSampler 裡的 seed 數字。把這個數字**鎖住**(取消隨機),只改文字裡的一個形容詞重跑一次 —— 你會發現構圖骨架大致不變,只有細節在變。這是聊天介面做不到的控制力。

> **已實測驗證**:這條流程已經幫你實際跑過一次,結果存在 `~/ComfyUI-遊戲美術教學/第一張測試圖.png`,用的 prompt 是:
> `a female game character concept art, wearing fantasy armor, standing pose, clean studio background, digital painting, highly detailed`
> 負向:`blurry, low quality, extra fingers, deformed, watermark`
> 你可以在瀏覽器介面打一樣的文字、選同一個 checkpoint(sd_xl_base_1.0),對照看看自己跑出來的跟這張像不像,確認環境完全沒問題。

---

## 第 4 章:Seed 與批次的概念

- Seed 隨機 = 每次都是全新的抽卡,適合「還在發想階段,想要多樣性」
- Seed 固定 = 微調同一張圖(換顏色、換光線、換小細節),適合「已經選定方向,要微調」
- ComfyUI 可以設定「batch」一次跑多張不同 seed 的圖,一次看多個版本,不用一張一張來回跟 AI 對話

---

## 第 5 章:圖生圖(img2img)—— 拿自己的草稿當底圖

流程:把 Load Checkpoint 之後的 Empty Latent Image 換成 **Load Image**(讀你的草稿)+ **VAE Encode**,再接到 KSampler。

**Denoise 參數是關鍵**:
- 0.3~0.4:大致保留你的構圖,AI 幫你上色/加細節
- 0.6~0.7:保留大致構圖,細節大幅改變
- 0.9 以上:幾乎等於重畫,只留一點點原圖影子

這一步可以把你手繪的草稿或角色設計圖,直接轉成上色完稿,而不是用文字重新形容一次。

---

## 第 6 章:局部重繪(Inpainting)—— 解決「來回改圖」的痛點

這是你原本聊天介面工作流程裡**最痛的環節**的解方。

流程:載入圖片 → 用內建的遮罩工具(滑鼠塗抹)圈出要修改的區域 → 送到 KSampler(搭配 inpaint 相關節點)→ 只有你塗的區域會被重新生成,其他地方原封不動。

**練習**:拿一張你之前用聊天介面產出、但某個地方畫壞的圖(例如手部崩壞),丟進來,只塗手的部分,重新生成,體會「其他地方完全不受影響」的差異。

---

## 第 7 章:精準構圖控制 —— ControlNet

> 目前教學驗證的是 SDXL 版 Canny(邊緣偵測),載入你的線稿/照片後會自動抓邊緣當構圖依據,`ControlNetApplyAdvanced` 節點上的 `strength` 越高,構圖跟參考圖越像。已實測跑成功；SD1.5 不能直接沿用這份 SDXL ControlNet graph。

**這是「精準度」問題的核心解方。**

流程概念:
1. 準備一張姿勢參考(可以是照片、簡筆骨架圖,甚至你自己的草稿線稿)
2. 加入 ControlNet 相關節點(需要額外下載 ControlNet 模型,+ Load ControlNet Model 節點),指定用 Pose(姿勢)、Depth(深度)或 Lineart(線稿)哪種控制方式
3. 生成結果會照著參考圖的構圖/姿勢走,文字只負責描述風格和細節

**這樣用**:你可以自己擺拍或簡單畫一個火柴人骨架,精準指定角色姿勢,而不是打字形容「舉起右手往前跨步」賭 AI 理解對不對。

> 這步需要額外下載 ControlNet 模型(依 SDXL 版本,一個約 2~5GB),建議先裝 ComfyUI Manager 再透過介面搜尋安裝,會比手動下載省事很多。
>
> **2026-08-17 更新**:Pose(骨架姿勢)、Depth(深度圖)這兩種 SDXL 控制方式已經裝好,`tools_src/generate.py` 的 `pose_only`/`character_action` task 現在支援 `--control-type canny|pose|depth` 參數選擇——`pose` 適合拿人物照片鎖動作(用 `comfyui_controlnet_aux` 的 `OpenposePreprocessor` 抽骨架,忽略衣服/背景細節),`depth` 適合鎖空間前後景關係。這些 add-on 只在 SDXL 家族 tier 有效；若偵測到 `sd15`，腳本會在上傳參考圖前拒絕這些路徑，不能只替換 checkpoint 檔名。

---

## 第 7.5 章:局部重繪 + ControlNet 結構鎖定 —— `guided_inpaint`

**解決純 Inpainting(第 6 章)在特定情境下的失敗模式**:遮罩範圍內如果同時要求「結構不能崩(手指關節、握姿、物體輪廓)」跟「外觀要換(武器造型、材質紋路)」,普通 inpaint 對遮罩內容完全沒有錨點,denoise 拉滿時結構跟外觀一起自由生成,失敗率會疊乘。

> **2026-08-18 實測紀錄**:幫女性角色換手持武器(碳鋼步槍 ↔ 狙擊槍,姿勢要維持一致)時,用純 `inpaint`(遮罩蓋住武器+雙手)連續失敗超過 5 次——武器造型變形、手指斷開、甚至出現懸空的肢體殘影。改用 `guided_inpaint`(疊加 `--control-type pose` 的 OpenPose ControlNet,鎖住手臂/手腕骨架)後,同樣的遮罩範圍第一次嘗試就結構正確,兩種武器都成功,才確認問題出在「結構沒有錨點」而不是遮罩位置或 prompt 措辭。

流程概念:跟 Inpainting(第 6 章)一樣要先圈出遮罩範圍,額外可以疊兩層可選的錨點:

- **結構鎖定(`--control-type`)**:預設直接從來源圖本身抽取結構,不用額外準備圖——`pose` 鎖手臂/關節骨架,適合「換手持物但握姿要對」這類需求;`canny`/`depth` 鎖輪廓邊緣/立體浮雕,適合「換材質紋路但造型要對」這類需求(例如同一把武器換不同稀有度材質貼圖、生物鱗片換紋路)
- **外觀參考圖(`--appearance-ref`,2026-08-18 補上)**:外觀不再只靠文字描述,改用一張圖決定——美術自己畫一張材質/紋理圖,想直接套到角色身上某個部位,這種「純文字講不清楚細節」的需求就是這個參數的用途。技術上是在遮罩範圍內疊一層 IPAdapter(跟第 8 章角色一致性用的是同一顆模型),參考圖建議是**乾淨的材質特寫**,不要整張場景照,不然背景/光影會一起被帶進來污染結果

兩層可以只用一層、也可以疊在一起(鎖形狀 + 換外觀圖,同時滿足「造型不變,材質換掉」)。

> **2026-08-18 補測**:拿一張生成的迷彩紋理當 `--appearance-ref`,套到角色背心上(疊 `canny` 鎖住背心外形),背心顏色/質感確實往參考圖的色系/風格靠攏,證實外觀參考圖這條路徑真的有效——但结果是「同色系條紋質感」,不是逐像素複製原本的像素化迷彩圖案。這是 IPAdapter 本身的特性(抓風格/色彩印象,不是圖案複製貼上),不要跟使用者保證能做到「一模一樣的紋理」。

**已知上限**:`pose` 目前只鎖到手腕/關節等級的骨架,沒有手指細節模型,能大幅降低「整個結構崩壞」的失敗率,但不保證每根手指都精確;遮罩範圍如果碰到肩章/臂章這類小圖案位置,即使當下是空白布料,還是有機會被模型重新腦補出圖案(這是 denoise=1.0 全自由生成的通病,不是 `guided_inpaint` 特有問題),真的踩到就用一般 `inpaint` 開一個小範圍單獨修。

---

## 第 8 章:角色與風格一致性 —— IPAdapter / LoRA

> 載入一張角色/風格參考圖,`IPAdapterAdvanced` 節點的 `weight` 控制貼合強度(建議先從 0.8 試),數值越高越像參考圖。已實測跑成功。LoRA 訓練屬於進階項目,還沒有現成流程(見下方附錄)。

**問題**:聊天介面每次產圖角色臉都不一樣,沒辦法用在同一個遊戲角色的不同姿勢/場景上。

**兩種解法,由輕到重:**

- **IPAdapter(快,不用訓練)**:丟一張角色參考圖進工作流,AI 會盡量貼合這張圖的臉/風格特徵,適合快速測試或還沒定案時用
- **LoRA(穩,需要前置訓練)**:把角色設定圖(建議 15~30 張不同角度)或你們團隊的美術風格圖集,訓練成一個小模型檔案,之後在 ComfyUI 裡直接套用,穩定度遠高於 IPAdapter,適合已經定案、要大量產出的正式階段

**建議順序**:定案前用 IPAdapter 快速測試方向,定案後投資時間訓練 LoRA,穩定產出整套遊戲素材。

### LoRA 訓練怎麼做(2026-08-17 補上,已實測驗證)

**訓練工具**:`kohya_ss`(裝在 `~/kohya_ss`,用 `uv sync` 建環境,`sd-scripts` 是它的 git submodule,實際跑訓練的是裡面的 `sdxl_train_network.py`)。這是跟 ComfyUI 完全獨立的另一套工具,只有要訓練 LoRA 時才需要,不是每台機器都要裝——參考 `skills/comfyui-install/SKILL.md` 的「進階:LoRA 訓練工具」段落。

**訓練資料準備**:一個資料夾(命名 `<重複次數>_<觸發詞>`,如 `20_角色名`),裡面放訓練圖 + 每張圖對應一個同名 `.txt` caption 檔,caption 裡包含你要的觸發詞。

**驗證方式**:拿 8 張本機產線自己生的、特徵很明顯的合成圖(桃紅盔甲 + 紫色漩渦紋章)當測試資料,訓練 160 步(dim=16,`--network_train_unet_only`,約 12 分鐘/RTX 4080),完整走過「資料準備 → 訓練 → 產出 `.safetensors` → 複製進 `models/loras/` → `generate.py --lora` 套用」全流程,結果:

- 用**完整訓練 caption** 生圖:桃紅盔甲 + 紫色漩渦紋章清楚重現,證明訓練真的學到東西
- 只用**觸發詞單獨**(不搭配任何特徵描述)生圖:**沒有**可靠觸發同樣的特徵

**已知限制,如實記錄**:這次用的是 SDXL LoRA 訓練官方建議的 `--network_train_unet_only`(只訓練 U-Net,不動文字編碼器),加上訓練量刻意壓到最小(160 步)只求驗證流程通不通。這個組合下,觸發詞本身的獨立記憶效果較弱,正式幫角色訓練 LoRA 時,建議:
1. 用真的角色照片(15~30 張,不同角度/姿勢),不是隨便湊的合成圖
2. 訓練量拉高(更多 epoch/步數)
3. 生圖時觸發詞旁邊還是搭配幾個關鍵特徵詞(而不是只丟觸發詞賭它記得),穩定度會高很多

`generate.py` 的 `concept`/`icon_asset`/`character_action`/`pose_only`/`style_lock` 五個 task 都支援 `--lora <檔名> --lora-strength <0~1,預設 0.8>`,沒指定就跟原本行為一樣。

---

## 第 8.5 章:放大精修 —— Upscale

> 透過 `generate.py upscale` 使用,2026-08-17 補上,已實測跑成功(1024×1024 → 2048×2048)。

**這是最後一道工序,不是產圖的一部分。** 前面幾章生出來的圖(`concept`/`character_action`/`refine` 這些)解析度都固定在 `device_config.json` 裡設定的預設值(這台機器是 1024×1024),平常拿來篩方向、比對構圖夠用,但真的要交件、要印出來、要當主視覺放大用時,解析度不夠、細節也會模糊。Upscale 做兩件事:

1. **放大模型**(這裡用的是 `4x-UltraSharp.pth`,適合插畫/概念圖風格):把圖片像素數變多,邊緣盡量保持銳利,比單純縮放好
2. **二次取樣補細節**(`--denoise` 控制強度,預設 0.4):放大完之後用同一個底模、同樣的 prompt 再跑一次擴散,補回原本解析度不夠時畫不出來的細節(材質、毛髮、盔甲紋路),單純放大模型本身不會生出新細節,這一步才會

**這樣用**:挑一張已經確定要用的圖,原封不動把當初生成用的 prompt 帶進來(這樣二次取樣時風格才會一致),指定要放大到幾倍(預設 2 倍),`denoise` 太低細節補不夠、太高會偏離原圖構圖,抓 0.3~0.5 之間微調。

---

## 第 8.6 章:小圖示與圖層拆分 —— icon_asset / layer_split

> 透過 `generate.py icon_asset` / `generate.py layer_split` 使用,2026-08-19 補上,已實測跑成功(RGBA 透明度驗證見下方)。

**這兩個 task 解決的是「單一小型 UI 素材」跟「複合元件要能分開疊放」這兩個需求,不是拿來生整個 UI 畫面**(整個畫面的版面配置仍然走 Figma MCP + design system 流程,不屬於這裡)。

1. **`icon_asset`**:生成單一小圖示/物件素材(按鈕圖示、道具圖示、複合元件裡的單一構件)。跟 `concept` 用同一套 graph,差異是固定在 prompt 尾端加一段「單一置中物件、無場景背景」的構圖引導詞,並且**永遠去背輸出透明背景**(沒有 `--remove-bg` 旗標可關)。構圖引導詞的附帶好處是去背品質也會變好——背景乾淨的單一主體,比「背景本身也有一堆裝飾元素在跟主體搶前景」的構圖更容易被去背模型正確分離乾淨。
2. **`layer_split`**:給一張已經定稿的完成圖 + 一張遮罩,依遮罩裁切出跟原圖等尺寸/位置對齊的透明圖層,**不重新生成畫面內容**。遮罩 alpha 慣例跟 `inpaint` 完全一樣(alpha=0 = 要保留進這一層,見第 6 章的遮罩踩坑說明),拆幾層呼叫幾次。

**複合元件(例如一個轉盤 UI 的外框/分區隔板/中心鈕)要怎麼拆成圖層,判斷原則**:結構相異的大塊(外框、中心鈕、指針)各自用 `icon_asset` 獨立生成一次;高度重複的元素(例如轉盤的每個分區隔板)**不要**逐一生成也**不要**事後切割,只生一片樣板交給美術自己在 Figma/引擎裡旋轉複製組成整圈——AI 獨立生成之間沒有像素級一致性保證,重複元素靠複製貼上遠比重複生成可靠。已經有定稿合成圖才用 `layer_split` 切大塊區域。判斷理由的完整說明見 `skills/comfyui-art-gen/reference/layered-assets.md`。目前沒有裝語意分割模型,不支援「AI 自動判斷圖層邊界」,圖層邊界一定要靠遮罩手動指定。

**`icon_asset` 的 `--structure-ref`:結構/顏色配置已有明確答案時的通用做法**。有些圖示的結構不該讓 AI 用文字自己瞎猜(例如「精確等分成 N 塊放射狀分區」這種計數幾何需求),`icon_asset` 支援 `--structure-ref <範本圖路徑>`,先準備一張已經畫好目標結構/顏色的範本圖,透過 img2img(denoise 鎖死 0.85)+ Canny ControlNet(strength 鎖死 0.85)把結構跟顏色配置都鎖住,SDXL 只負責疊材質/光澤,不負責決定「有幾塊、顏色怎麼排」。這不是轉盤專屬功能——放射狀等分只是其中一種常見情境,範本圖可以是任何來源(使用者提供的草圖,或程式產生,`tools_src/generate.py` 裡的 `build_wheel_segment_template()` 是現成的放射狀範本產生輔助函式,agent 自己 import 呼叫,不是獨立的 CLI 選項)。**已知取捨**:2026-08-19 實測(轉盤案例)denoise 從 0.55 調到 0.85 結構顏色都穩定,質感也持續提升,但鑲花雕紋這類需要額外邊緣線條的裝飾細節,不管怎麼調都沒有明顯出現——推測是 Canny ControlNet 鎖邊緣同時也壓抑了「多畫額外線條」,是結構性限制,不是沒調好,細節見 `skills/comfyui-art-gen/reference/structure-ref.md`。

**已知踩坑(2026-08-18,順手修的既有 bug,不是這次新增能力本身的限制)**:`--remove-bg` 曾經有個真實 bug——`RemoveBackground` 節點輸出的 MASK 語意是「1=前景主體」,但接去背輸出的 `JoinImageWithAlpha` 節點內部會把傳入的 alpha 再做一次 `1-x` 換算,兩者語意相反,直接接會讓主體變透明、背景保留(方向整個接反)。已經照 ComfyUI 官方「Remove Background (BiRefNet)」blueprint 的做法,在中間插入 `InvertMask` 節點修正,現在 `--remove-bg`(以及 `icon_asset` 內建的去背)都已經修好並實測驗證過(alpha histogram 確認主體不透明、背景透明)。另外 `attach_bg_removal()` 組節點 id 時也修過一個相關 bug:原本假設所有節點 id 都是純數字字串,但 `LoraLoader` 固定用 `"1b"`、`icon_asset` 的 ControlNet 分支用 `"4a"`/`"4b"`/`"4c"` 這類字母尾綴 id,對這些 key 呼叫 `int()` 會直接噴例外(`--structure-ref` 疊 `--remove-bg` 時發現),已改成只挑純數字 key 取最大值。

---

## 第 8.7 章：CLI 設定、離線檢查與版本鎖定

### 執行位置與 ComfyUI URL

`generate.py` 會部署到 `<ComfyUI 安裝路徑>/tools/generate.py`，但原始碼以 repository 的 `tools_src/generate.py` 為準。執行時要依 `local_config.json` 帶入實際的 `comfyui_url`：建議每個 task 都明確使用 `--comfy-url <URL>`，或使用 `--config <local_config.json>` 指定含 `comfyui_url` 的 JSON；也可以設定 `COMFY_URL`/`COMFYUI_URL`。解析順序是 CLI URL → URL 環境變數 → 明確指定的 config，**不會自動搜尋或猜測 `local_config.json`，也不會預設連 8188**。`--timeout <秒數>` 是 prompt 送達 ComfyUI 後輪詢生成結果的上限，必須是有限正數；上傳、送出與下載各自另有不超過 30 秒的 HTTP request timeout，所以它不是整支 CLI 的 wall-clock 上限。CPU、batch、upscale 可依實際耗時調高，逾時後先查 ComfyUI 狀態，不要未確認就重送相同任務。

常用的無 GPU 檢查在 repository 根目錄執行：

```bash
python -m compileall -q tools_src tests
python -m unittest discover -s tests -p 'test_*.py' -v
```

這些檢查只驗證 Python 語法、graph、參數界線與 HTTP 邊界，不等於真的載入模型或產圖；CI 也只執行這兩類檢查，不會把 workflow 檔案或模型下載進 repository。

### 參數界線與 SD1.5 限制

送出前會拒絕不合法參數：寬高必須是正整數且為 8 的倍數、`batch >= 1`；`denoise`、`ip-weight`、`pose-strength`、`control-strength`、`appearance-weight`、`lora-strength` 都在 `0..1`；`scale` 必須 `>0` 且 `<=4`；`timeout` 必須是有限正數。`steps`、`cfg`、sampler 與 scheduler 仍是固定值，沒有開放 CLI 旗標。

`sd15` tier 目前只承諾 `concept`、`refine`、一般 `inpaint`、`upscale` 與不帶參考圖的 `icon_asset` 基礎路徑。`pose_only`、`style_lock`、`character_action`，以及 `icon_asset` 的 `--structure-ref`/`--appearance-ref`、`guided_inpaint` 的 ControlNet/IPAdapter 都需要 SDXL 家族；`--style` 三個風格底模也只支援 SDXL。腳本會在上傳參考圖或建立佇列前擋下不相容組合，不能只把 SDXL checkpoint 換成 SD1.5 就宣稱支援。

### 版本與交付狀態

`docs/tested-versions.md` 是版本 manifest 的格式與狀態說明，記錄同一台已安裝機器的 ComfyUI/custom node commit、Python/PyTorch/Pillow 版本、實際模型 SHA-256，以及 smoke test 日期、指令與輸出。未在已安裝機器擷取的值保持 `null`；不能把「目前最新版」或網路上的檔名當成已驗證版本，也不能捏造 SHA。`local_config.json` 不進 Git，因此 clean clone 仍不會自帶本機路徑；XU-Nano-PC 的實際 capture 以 manifest 為準，其他機器仍需各自偵測與 smoke。

`workflows/` 是本機維護與除錯用的產物，已由 `.gitignore` 排除，不會隨 repository clone 提供；要給另一台機器使用，請從已安裝的 ComfyUI `user/default/workflows/` 另行複製或匯出 JSON。影片能力沒有把 workflow JSON 納入版控，因此 checklist 的 workflow 同步為 N/A。

## 第 8.8 章:讓靜幀動起來 —— img2video

> 透過 `generate.py img2video` 使用。2026-08-26~27 的 Windows / RTX 4080 裝機與實測是歷史紀錄，clean clone 不會自帶那些模型；XU-Nano-PC 的精確版本、PyAV 與模型 hash 已記在 [`docs/tested-versions.md`](docs/tested-versions.md) 的 verified manifest，其他機器要各自重新 capture。操作流程見 `skills/comfyui-video-gen/SKILL.md`,設計背景見 `DESIGN.md`。

這不是「會動的 concept」。影片用的是 Wan 2.2 / MiniMax H3,跟 SDXL 的 ControlNet/IPAdapter/`--style` **不相容**,不要把現有圖片 task 加一個旗標假裝能產影片。

**backend 由 capability config 決定:**這台 4080 的歷史對打中 H3 保原圖(帽子/臉/道具)明顯比較好,而且輸出有立體聲；若 machine config 明確把 H3 設為 default，可省略 `--backend`。若 config 沒有 default，就必須明確給 `--backend h3` 或 `--backend wan`；`wan` 走 Wan 2.2 5B,比較快、無聲、身份比較容易漂。影片 backend 不跟圖片 tier/`--style` 混用。

**已實測(2026-08-26,不要自動播成品):**

| | Wan 2.2 5B | MiniMax H3 |
|---|---|---|
| 832x480、約 2 秒、20 steps | 42 秒出片,無聲 | 66 秒出片,有聲音、較像原圖 |
| 768x768、約 6.6 秒 | — | 409 秒出片,`output/witch_idle.mp4`(H.264 + AAC,一般播放器可播) |

時長鎖在 2~6 秒,更長拆鏡。畫布跟來源圖比例走,最長邊上限 768。`img2video` 預設只產 mp4、不抽 png；需要序列時才加 `--extract-frames`。LTX-2.5 本輪沒裝。

其餘已上線並實測(2026-08-27,轉盤素材、未自動播放；這些是歷史輸出，不取代目前的 machine-specific capability/manifest 驗證):

- **`fx_loop`**:首尾同一張圖鎖閉環,預設抽 png。`fx_loop_h3_00001_.mp4` 56 幀,首尾平均像素差約 1.4(很接近閉環),並抽出 `fx_loop_h3_00001__frames/`
- **`transition`**:兩張靜幀中間那段。`transition_h3_00001_.mp4`(tier1 → tier2)
- **`clip_extend`**:吃上一支 mp4 最後一幀再往後生。`clip_extend_h3_00001_.mp4`
- **`video_concat`**:多鏡接成一支。五鏡實測 `output/wheel_short_20s.mp4`(768x768, 20.2 秒, H.264,未自動播放)

有劇情的片子:先寫鏡頭表再逐鏡呼叫,最後用 `video_concat` 做基本串接，字幕/配樂/完整時間線交給外部工具。操作見 `skills/comfyui-video-gen/SKILL.md`。

**`character_video`(2026-08-27 已實測,不要自動播成品):**對應靜態 `style_lock`,不是 `img2video` 加旗標。參考圖鎖身份,模型組新鏡頭,第一幀不必是那張定稿圖。task 名不綁模型;這台歷史上用 H3 的實作細節見 `skills/comfyui-video-gen/reference/backends.md`，現行選擇以 capability config 為準。不能接 SDXL IPAdapter。操作見 `skills/comfyui-video-gen/SKILL.md`。

煙測:`output/female_carbine_final.png`(灰棚全身站姿)→ prompt 改成倉庫裡步槍下垂走向鏡頭。牆鐘 **69.6 秒**,`output/character_video_h3_00001_.mp4`(512×768、56 幀、2.33 秒、H.264 + AAC 立體聲)。第一幀對來源靜幀平均像素差約 89(不是 I2V 那種「還是那張棚拍」),服裝/髮型/背心仍認得出是同一人,背景跟走位是新鏡頭。抽幀在 `character_video_h3_00001__frames/`(56 png,未自動播放)。

**`camera_move`(2026-08-27 已實測,不要自動播成品):**主體盡量靜止、只運鏡。`--camera` 鎖死枚舉。有 last_frame 能力的 backend 會再餵幾何終點靜幀。操作見 `skills/comfyui-video-gen/SKILL.md`,取捨見 `skills/comfyui-video-gen/reference/camera-move.md`。

同一張轉盤 `--camera zoom_in` 對照:只靠 prompt 的 `camera_move_h3_00001_.mp4` 首尾像素差 16;改成幾何終點靜幀當 H3 尾幀後 `camera_move_h3_00002_.mp4` 首尾差 **41.8**,末幀對 `_camera_end.png` 平均差 **3.9**(幾乎貼齊),牆鐘 106.8 秒,768×768、56 幀、H.264+AAC。抽幀 `camera_move_h3_00002_frames/`。`video_concat` 兩支 H3 短片接成 `camera_concat_audio_test.mp4`(112 幀、4.67 秒,**立體聲 AAC 時長對齊**,未自動播放)。

**`pose_drive`(2026-08-27 已實測,不要自動播成品):**對應靜態 `character_action`。角色靜幀 + 動作參考影片,而且**靜幀姿勢必須接近動作片第一幀**。歷史煙測走 H3；現行會依 capability config 選擇，不會在不支援時自動掉到 Wan。操作見 `skills/comfyui-video-gen/SKILL.md`。

對的綁法:抽走路片第一幀當角色圖 + 同一支走路片。預設 h3 `output/pose_drive_00004_.mp4`(512×768、56 幀、2.33 秒、H.264+AAC,154.6 秒),首幀對靜幀平均像素差 4.1,畫面是一個人走向鏡頭,中後段還是同一張臉。同輸入的 wan `00003_` 較快無聲(94.4 秒、49 幀),但中後段臉會漂。抽幀 `pose_drive_00004_frames/`。

跨角色:同一支走路片套到兩個不同人(先 `character_action` 擺成走路起點)。騎士 `output/pose_drive_00005_.mp4`(208.8 秒)、法師 `output/pose_drive_00006_.mp4`(151.4 秒),都是 512×768、56 幀、H.264+AAC,倉庫走向鏡頭、全程一人。沒有自動播放。

錯的綁法(不要再用):棚拍站姿 `female_carbine_final.png` 去套走路片 → `pose_drive_00001_.mp4` 雙人。那不是介面壞掉,是姿勢對不上。

---

## 第 9 章:建立你的生產線 SOP

1. 把調好的工作流用右上角 **Save** 存成 `.json`,放進已安裝機器的 `~/ComfyUI/user/default/workflows/`（Windows 對應 `%USERPROFILE%\ComfyUI\user\default\workflows\`）,下次打開 ComfyUI 直接從選單載入
2. 把這個 JSON 另行分享給組員,大家用同一套流程,確保產出風格一致、可回溯——**組員不需要學會拉節點**,拿到 JSON 打開後只要換文字/換參考圖、按 Queue 就好,跟操作一個現成工具沒兩樣。`workflows/` 不由 repository clone 提供，不能把 clone 後找不到該檔案視為安裝失敗；需要新增/調整節點結構(例如換模型、加新的控制方式)才是本文件負責的範圍
3. 需要一次產一批素材(例如 10 種武器材質變體)時,用 batch 功能一次排隊生成,不用一張一張手動跑

---

## 第 10 章:Mac 使用注意事項

- Mac 用 MPS(蘋果的 GPU 加速)跑,沒有 N 卡的 xformers / flash-attention 加速,同樣工作流速度會比同級距 N 卡慢,這是先天限制
- 36GB 統一記憶體對 SDXL、ControlNet、輕量影片模型(如 Wan2.2 5B)都夠用,但同時開很多大型模型或跑 14B 級影片模型會吃緊,建議一次專心跑一個大任務
- 每次重開機後,啟動 ComfyUI 記得用第 1 章的指令重新啟動伺服器

---

## 附錄:環境安裝紀錄(2026-07-29)

已經裝好、可以直接用的東西:

| 章節 | 項目 | 存放位置 |
|---|---|---|
| 第 3~6 章 | SDXL 底模 | `~/ComfyUI-Shared/models/checkpoints/sd_xl_base_1.0.safetensors` |
| 第 7 章 | ControlNet(Canny, SDXL 版) | `~/ComfyUI-Shared/models/controlnet/controlnet-canny-sdxl-1.0.safetensors` |
| 第 8 章 | ComfyUI_IPAdapter_plus(custom node) | `~/ComfyUI-Installs/ComfyUI/ComfyUI/custom_nodes/ComfyUI_IPAdapter_plus` |
| 第 8 章 | CLIP Vision 編碼器 | `~/ComfyUI-Shared/models/clip_vision/CLIP-ViT-H-14-laion2B-s32B-b79K.safetensors` |
| 第 8 章 | IPAdapter 模型(Plus, SDXL 版) | `~/ComfyUI-Shared/models/ipadapter/ip-adapter-plus_sdxl_vit-h.safetensors` |

版本與安裝狀態補充:

| 項目 | 說明 |
|---|---|
| LoRA 訓練工具(如 kohya_ss) | 是否安裝依機器而異；這台歷史快照記錄曾完成流程，但不代表乾淨 clone 已具備，請以實際機器與版本 manifest 為準 |
| 其他 ControlNet 類型(Pose、Depth) | 這台 Windows 歷史快照記錄曾安裝 SDXL 版本；SD1.5 不可直接沿用，其他機器需依 tier 另行安裝與實機驗證 |

本附錄是歷史安裝紀錄，不是可直接複製的版本鎖定清單；要重建環境請先看 [`docs/tested-versions.md`](docs/tested-versions.md) 與 `skills/comfyui-install/SKILL.md`。目前 repository 沒有本機 `local_config.json`、ComfyUI 或模型檔，無法在這裡執行實機 smoke/deploy。

**下一步建議**:在已安裝機器完成 `local_config.json`、版本 manifest 與 smoke test 後,再照第 2、3 章實際跑出第一張圖；本 clean clone 目前只能先完成離線檢查。
